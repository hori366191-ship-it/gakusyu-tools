# 学習ツール 移行仕様書（ハイブリッド静的化案）

最終更新: 2026-08-30
元仕様: `仕様書.md`（2026-08-25版） / `AGENTS.md`

---

## 0. 本書の位置づけ

GASの課題（ペアレンタルブロック・上部バー・多ログイン `/u/N` エラー）を解消するため、**生徒向け表示は匿名静的ホスティング（GitHub Pages）へ、教室内特権機能だけを不可視GAS APIとして残す** ハイブリッド構成への移行仕様。

- **全面GAS脱却ではなく、費用0・新インフラなし** で現行Drive/鍵運用を維持する案
- 本書が `仕様書.md` を置き換えるまで、現行 `仕様書.md` は「GAS版」の正とする

---

## 1. 移行の狙い

| 課題 | 現行GAS | 移行後 |
|---|---|---|
| ペアレンタルコントロールで `accounts.google.com` がブロック | `app2` 等はログイン必須のため開けない | 閲覧は匿名Pagesなのでブロックされない。生成/印刷だけがログイン要求 |
| 上部グレー警告バー | `HtmlService` 配信では常時表示 | Pages配信では出ない。GASはJSONP/POSTのAPIとして叩くためバーは表示されない |
| 多ログイン `/u/N` エラー | `/a/` 形式で回避しているが依然不安定 | PagesはGoogle外なので発生しない |
| 初回表示の遅さ (`app1` で顕著) | GASコールドスタート + Drive fetch で2〜5秒 | `app1` を完全静的化し `bundle.json` をCDN配信。0.3秒台 |

---

## 2. 全体アーキテクチャ

```
[生徒スマホ/自宅PC - 匿名]
  └─ https://<pages>/portal, app1, app3-8, app9(閲覧), app2(閲覧)
     は全て GitHub Pages（無料・ログイン不要・バーなし）

[教室タブレット / 先生PC - 要ログイン]
  └─ 同じPages上の「教室タブレットで印刷」「英語長文を生成」ボタン
     が、GASへの ?probe=1 でログイン検知できた場合のみ表示
     │
     ├─ 原稿PDF ──iframe POST──▶ [GAS pdfdrop] ──▶ Driveフォルダ
     └─ 生成要求 ──fetch/POST──▶ [GAS app2-backend] ──▶ Drive保存
```

### 2.1 ホスティング分担

| 区分 | 配置先 | 対象 | 認証 |
|---|---|---|---|
| **静的フロント** | GitHub Pages（推奨。Cloudflare Pages/Vercelでも可） | `portal`, `app1`(ビルド成果物), `app3`, `app4`, `app5`, `app6`, `app7`, `app8`, `app9` 本体, `app2` 閲覧部 | なし（匿名） |
| **不可視API** | **新規GAS2本を作成**（既存 `apps/` のGASは全て残置） | `site-pdfdrop`（新規）, `site-app2-backend`（新規） | `DROP_TOKEN` + `ALLOWED_EMAILS`（下記） |

> 静的化しても新 `site-pdfdrop` / `site-app2-backend` のデプロイURLは `script.google.com/a/macros/hori-shota.com/s/.../exec`（新デプロイID）。生徒は直接触れず、JSが裏で呼ぶ。既存 `apps/app2` / `apps/pdfdrop` の旧GAS・旧デプロイURLは永久に残置し、旧ポータル経由の旧アクセスも維持。

---

## 3. 認証仕様（共通）

### 3.1 方針

- **閲覧は全員匿名可**。ログインしていなくても教材・シミュレータは使える。
- **書込み（生成・印刷・削除）だけが「教室内限定」**。Pages上のボタンは `?probe=1` で可否を判定し、条件を満たさなければ **最初から非表示**。

### 3.2 アカウント構成

| 端末 | アカウント | `Session.getActiveUser().getEmail()` の可視性 | 扱い |
|---|---|---|---|
| 先生PC | `hori-shota.com` (Workspace) | 可視 | `ALLOWED_EMAILS` で許可 |
| 教室タブレット | 教室用Gmail（別アカウント） | **不可視**（GAS仕様で空文字） | `TOKEN` のみで許可 |
| 生徒スマホ | 未ログイン or 個人アカウント | 空 or リスト外 | ボタン非表示 |

### 3.3 ゲートロジック（pdfdrop と app2-backend 共通）

`apps/pdfdrop/Code.js:63` と同一：

```js
function isAllowed_(email){
  var list = getAllowedList_(); // ALLOWED_EMAILS
  if (!email) return true;      // 不可視はTOKENだけで通す
  if (!list.length) return true;// リスト空ならTOKENだけで通す
  return list.indexOf(email) !== -1;
}
function gate(token, email){
  if (token !== DROP_TOKEN) return 'bad token';
  if (!isAllowed_(email)) return 'not allowed';
  return 'ok';
}
```

- `DROP_TOKEN` は既存 `apps/pdfdrop/Code.js:1` / `apps/app9/index.html:299` の `e6135dfd707ab9a6916635fa` を**流用**（新GAS2本と `site/app9` / `site/app2` で共通）。漏洩時は新GAS2本の Script Property と Pages側定数を同時更新。既存GASの `DROP_TOKEN` も同値のまま残置。
- `ALLOWED_EMAILS` は可視アカウント（先生PC）のみを列挙。教室Gmailは不可視のため列挙不要。新旧GASとも同一リストをコピー。
- `MAX_PER_DAY` / `MAX_BYTES` / `%PDF` 検査は `site-pdfdrop` のみ維持（旧 `apps/pdfdrop` も同設定のまま残置）。

### 3.4 probe による条件表示

`apps/app9/index.html:423` / `apps/pdfdrop/Code.js:146` と同型を `app2` と `portal` にも移植：

- Pages側は起動時に `GET <GAS_URL>?probe=1&token=...&callback=__cb` をJSONPで発射（4秒ウォッチドッグ）。
- GASは `{genkoProbe:true, ok, email_visible, masked, allowed}` を返す。
- 判定と表示は下記3状態で行う（文言はユーザ指定）：
  - `!ok`（タイムアウト/無応答）→ 未ログイン：ボタン非表示。ポータルでは「教室タブレットとしてログインする」ボタンを表示
  - `ok && allowed && !email_visible` → 教室タブレット（不可視Gmail）でログイン中：`app9`/`app2`の特権ボタンを表示、ポータルでは「教室タブレットとしてログイン中」表示
  - `ok && allowed && email_visible` → 先生アカウント（hori-shota.com）でログイン中：同様に特権ボタン表示、ポータルでは「先生アカウントでログイン中」表示
  - `ok && !allowed` → 教室外アカウントでログイン中：特権ボタン非表示、ポータルでは「教室外アカウント」表示（恐怖感を避けた文言）
- `app9` は現行で「クリック時にprobe」だが、**移行後は「起動時にprobe」に変更**（押してから気づく体験を解消）。

---

## 4. 各アプリの移行後仕様

### 4.1 portal（新・Pages版）

- **作成**: `apps/portal/index.html` を `site/index.html` に複製してPages用に改修。`Code.js` は不要（Pagesは静的）。
- **変更**: `qrUrl` (`index.html:544`) と各カードの `href` を Pages URL に差替。GAS `/a/` 形式は廃止。`qrcode-generator` はインラインのまま。
- **追加 — ログイン状態表示**: ヒーロー下または `notice` 直上にステータス領域を新設。起動時に新 `site-pdfdrop`（または `site-app2-backend`）へ `?probe=1` をJSONP発射し、上記3.4の4パターンで表示を切替。
  - 未ログイン → 「教室タブレットとしてログインする」ボタン（新GASの `/a/` URLへ `target="_blank"`。ログイン後にポータルをリロードすると「ログイン中」に遷移）
  - 教室タブレット（不可視）→ 「教室タブレットとしてログイン中 ✅」
  - 先生アカウント（可視）→ 「先生アカウントでログイン中 ✅」
  - 教室外アカウント → 「教室外アカウント」表示（特権ボタンは出さない・恐怖感を避けた文言）
  - 判定中は「確認中…」のプレースホルダ。`prefers-reduced-motion` 等は既存ヒーローに準拠。
- **効果**: バー消失・ペアレンタル耐性。セクション/教科色は維持。生徒は一目で教室内権限の有無が分かる。

### 4.2 app1（公式・暗記辞典）

- **移行**: **完全静的ビルド**を採用（API残置はしない）。
- **理由**: `DriveApp + CacheService` のバンドル方式は初回2〜5秒かかる。Pagesで `bundle.json` を事前生成すれば0.3秒台。
- **ビルド**: リポ内に `apps/app1/content/*.md` を置き、GitHub Actions で `DRIVE_FOLDER_ID=1wrajAftZ5r5i0Yn5SNlhMq1fG-rDIVS3` 相当を `content/` へ同期 → `marked` で `bundle.json` 生成 → Pages配信。`Index.html` の `fetch(?action=getBundle)` は `fetch(./bundle.json)` に差替。`localStorage mdv1_bundle` キャッシュは維持。
- **運用変更**: Driveで直す → GitHubで直す（教室長のみ）。生徒体感は同一。

### 4.3 app3〜app8（シミュレータ群）

- **移行**: 各 `index.html` をそのまま Pages へ。`Code.js` は全て廃止（6行の `doGet` のみだった）。
- **対象**: `app3` 正規分布 / `app4` 立体 / `app5` 原子 / `app6` 微分積分 / `app7` 月星座 / `app8` 英文法
- **外部依存**: `KaTeX 0.18.4` / `Google Fonts` のCDNはそのまま。`google.script.run` は0件のため差替なし。

### 4.4 app9（原稿用紙メーカー）

- **移行**: 本体は Pages へ。`Code.js` 廃止。
- **変更点1（条件表示）**: `apps/app9/index.html:492` の即時表示を撤廃し、起動時 `probeAuth` で `教室タブレットで印刷` ボタンの表示を決定。`isGasEnv` 判定は残す（`file://` ではprobeしない）。
- **変更点2（送信）**: `CLASSROOM.url/token` は現行 `pdfdrop` の `/a/` URL のまま。`html2canvas+jsPDF` → `iframe POST` → `?poll` ポーリング（3秒×20回）の流れは維持。Pagesからでも `script.google.com` への `form POST` は許可されるためCORS不要。
- **保存先**: `pdfdrop` の `DROP_FOLDER_ID` フォルダのまま。

### 4.5 app2（英語長文 多読リーダー）— 閲覧部

- **移行**: 閲覧・辞書引きは Pages へ。`index.html` の `IS_GAS=false` で配信し、`SERVER` 定義 (`index.html:635`) の `fetch` パスを `app2-backend` GAS URL に向ける。
- **辞書**: `DICTIONARY_ID=13G_bvPbBaoFx-XC9qJAaJTBP66G_-EdK` / `MAP_ID=1J4_grvw-tZKV1BLqgVlCGb8A5Oa2hEdx` はビルド時に JSON として Pages に同梱（Pagesから直配信しGAS呼出を削減）。GAS側の `getDICTIONARY` / `getFormMap` はフォールバックとして残す。

### 4.6 site-app2-backend（生成・保存・削除）— 新規不可視GAS

- **母体**: 新規GAS `site-app2-backend` を `clasp create`（`hori-shota.com`）で作成。現行 `apps/app2/コード.js` を複製して初期コードとする。既存 `apps/app2`（`YOUR_SCRIPT_ID...`）は一切改変せず永久残置（旧ポータル経由の旧URL用）。
- **変更**（新GAS側で実施）:
  1. `isAllowedUser_()` を `pdfdrop` 型に変更（`!email → true`）。`ALLOWED_EMAILS` は可視の先生PCのみ列挙。
  2. `DROP_TOKEN` を Script Property に追加（既存 `e6YOUR_SCRIPT_ID...` を流用。`site-pdfdrop` と共通）。
  3. `doPost(e)` を拡張：`action=generateText|deleteText` に加え `token` 検証を先頭で実施。`generateText` は `UrlFetchApp` → `DriveApp.createFile` の流れは維持。
  4. `doGet(e)` に `?probe=1` / `?poll` 相当を追加（`apps/pdfdrop/Code.js:146` の `handleProbe_` を移植）。`getSavedTexts` は `GET ?action=getSavedTexts&token=...&callback=` のJSONPでも取得可能にする（PagesからのCORS回避）。
- **Script Properties**（新GAS）: `OPENCODE_API_KEY`, `ALLOWED_EMAILS`, `OWNER_EMAIL`, `FOLDER_ID`, `DROP_TOKEN` を既存 `apps/app2` からコピー。`DICTIONARY_ID` / `MAP_ID` はコード内定数のまま。
- **保存先**: 既存と同一の `FOLDER_ID`（未設定なら「生成済み長文」）を指す。`tadoku-*.json` 形式も同一。旧GAS生成分と新GAS生成分は同一フォルダに混在する（並行稼働）。
- **削除**: `isOwner_()` は維持（`apps/app2/コード.js:58`）。`OWNER_EMAIL` は先生PCのみ。教室タブレット（不可視で `!email → false`）は現行通り削除不可。移行後もこの権限差は維持する。
- **デプロイ**: 新GASで `アクセス: Googleアカウントでログイン / 実行: 自分` をUIで設定（`clasp deploy` 禁止）。

### 4.7 site-pdfdrop（PDF受信）— 新規不可視GAS

- **母体**: 新規GAS `site-pdfdrop` を `clasp create` で作成。現行 `apps/pdfdrop/Code.js` を複製して初期コードとする。既存 `apps/pdfdrop`（`YOUR_SCRIPT_ID...`）は一切改変せず永久残置（旧 `app9` からの旧投函用）。
- **Script Properties**（新GAS）: `DROP_TOKEN`（`e6YOUR_SCRIPT_ID...` 流用）, `DROP_FOLDER_ID`, `ALLOWED_EMAILS`, `MAX_PER_DAY` を既存からコピー。`LAST_OK/LAST_ERROR/COUNT` は新GASで独立管理。
- **保存先**: 既存と同一の `DROP_FOLDER_ID` を指す。新旧どちらの `pdfdrop` からの投函も同一フォルダに保存され、印刷監視は従来通り。
- **移行後の呼出**: `site/app9` は新 `site-pdfdrop` の `/exec` を `CLASSROOM.url` として参照。旧 `apps/app9` は旧 `apps/pdfdrop` を参照したまま。
- **デプロイ**: `アクセス: Googleアカウントでログイン / 実行: 自分` をUIで設定。

### 4.8 旧ポータルGASの移行後改修（検証完了後に実施）

- **対象**: 既存 `apps/portal`（`YOUR_SCRIPT_ID...`）の `index.html`。**基本機能はそのまま残しつつ**、移行バナーを追加するのみ。現行カードのリンクは**旧GASのまま残す**（張替えない）。
- **バナー仕様**:
  - 位置: ヒーロー直下（`header.hero` の直後、`main` の直前）、全幅。
  - デザイン: アンバー系（`#fffbeb` 地 + `#f59e0b` 左ボーダー6px + `notice` と同系だがより強調）。アイコンは `notice` と同様のGoogle的アイコンは不要、シンプルな `↗` 付き。
  - 文言（ユーザ指定）:
    ```
    学習ツールポータルは新しいURLに移行しました。
    以下のリンクから新しいサイトへ進み、ブックマークし直してください。
    https://hori366191-ship-it.github.io/gakusyu-tools/ （新URL）
    ```
    - 2行目に新URLを大きなリンクボタン（`card__btn` と同色）で表示。`target="_blank"` + `rel="noopener"`。
  - QRは旧GAS URLのまま残置（旧ポータル自体のQRのため）。新ポータルのQRは新URLを指す。
- **運用**: バナー追加は `apps/portal` の `clasp push` → UIでデプロイ更新（新バージョン）のみ。旧GASのデプロイURLは不変。旧ポータルは永久にアクセス可能だが、新規利用は新URLへ誘導。

---

## 5. URL・リポジトリ構成（移行後）

### 5.1 公開URL

| 旧（GAS） | 新（Pages） | 備考 |
|---|---|---|
| `https://script.google.com/a/macros/hori-shota.com/s/.../exec` (10本) | `https://hori366191-ship-it.github.io/gakusyu-tools/<app>/` | Pagesのカスタムドメイン設定時は `https://tools.hori-shota.com/<app>/` に置換可 |
| `pdfdrop`, `app2-backend` の `/exec` | 旧URLのまま残置（Pagesから裏で呼ぶ） | 生徒には非公開 |

### 5.2 リポジトリ構成（案）— 現行 `apps/` は永久残置

ユーザ指定により、実装時は現行 `apps/` を直接改変せず、別フォルダ・別GASに新規作成する。移行後も `apps/` と旧GASは永久に残す。

```
E:/dev/学習ツール/
├── site/                         ← ★新規：Pages配信の正本（現行 apps/ とは分離。永久に併存）
│   ├── index.html                ← portal（apps/portal/index.html を複製してPages用に改修）
│   ├── app1/index.html + bundle.json
│   ├── app3/index.html … app8/index.html  ← 各 apps/appN/index.html を複製
│   ├── app9/index.html           ← 起動時probe版（新 site-pdfdrop を参照）
│   └── app2/index.html           ← 閲覧部のみ（IS_GAS=false版。新 site-app2-backend を参照）
├── site-src/app1/content/*.md    ← 辞典の正本（ビルド元。apps/app1/content とは別管理）
├── site-gas/                     ← ★新規：Pages用GAS2本（新scriptId。既存 apps/ のGASとは別）
│   ├── site-pdfdrop/             ← 新規作成（apps/pdfdrop/Code.js を複製）
│   └── site-app2-backend/        ← 新規作成（apps/app2/コード.js を複製してAPI化）
├── apps/                         ← ★現行GAS版は永久残置（一切削除しない）
│   ├── portal, app1, app2, app3, app4, app5, app6, app7, app8, app9, pdfdrop
│   └── 全ての Code.js / .clasp.json / appsscript.json を維持。旧デプロイURLも維持
├── .github/workflows/pages.yml   ← site/ をPages配信 + app1 bundle生成
└── scripts/backup.ps1            ← 維持
```

- Pages配信ルートは `site/`（`docs/` でも可だが `site/` で `apps/` と明確に分離）。GitHub Pages 設定は `Branch: master / site` または `gh-pages` ブランチ。
- 現行 `apps/` と旧GAS（11本）は移行完了後も**永久に残置**。削除・停止しない。旧ポータルは下記 `4.8` のバナー追加のみ行う。新旧は同一 `DROP_TOKEN` / 同一 `DROP_FOLDER_ID` / `FOLDER_ID` を共有するため保存先フォルダも共通。
- `scripts/backup.ps1` の Drive bundle バックアップは Pages 移行後も維持（Pagesはgit pushでデプロイされるため）。

---

## 6. 移行手順

### Phase 0 — 静的7本のPages化（作業1h・リスク0）

1. `site/` フォルダを新規作成し、`portal, app3, app4, app5, app6, app7, app8` の `index.html` を `site/` へ複製（現行 `apps/` は改変しない）
2. `site/index.html`（portal）の `qrUrl` とカード `href` を Pages URL に差替。ログイン状態表示領域を追加（3.4の4状態）
3. GitHubで Pages を `Branch: master / site` で有効化
4. `curl -I https://.../portal/` で200確認。既存GAS版は永久に並行稼働（停止しない）

### Phase 1 — app1 完全静的化

1. `site-src/app1/content/`（または `site/app1/content/`）に `.md` を移行（またはDrive→content同期スクリプトを一時実行。現行 `apps/app1/` は残置）
2. `pages.yml` で `site/app1/bundle.json` 生成 → Pagesデプロイ
3. `site/app1/index.html` の `fetch` 先を `bundle.json` に差替

### Phase 2 — app9 条件表示化 + 新GAS作成

1. `site-gas/site-pdfdrop` を新規GASとして `clasp create`（`hori-shota.com`）。`apps/pdfdrop/Code.js` を複製して `clasp push`。Script Properties（`DROP_TOKEN` は `e6135...` 流用、`DROP_FOLDER_ID` 等）を既存からコピーし、UIでデプロイ（`Googleアカウントでログイン/自分`）
2. `apps/app9/index.html` を `site/app9/index.html` に複製し、`492` 行付近を「起動時probe」に書換。新 `site-pdfdrop` の `/exec` を `CLASSROOM.url` に設定（現行 `apps/app9/` は残置し旧 `pdfdrop` を参照したまま）
3. Pagesへ配置し、教室タブレット（Gmail）と先生PCでボタン表示、未ログイン/教室外アカウントで非表示を確認

### Phase 3 — app2-backend 新規GAS化

1. `site-gas/site-app2-backend` を新規GASとして `clasp create`。`apps/app2/コード.js` を複製し、`DROP_TOKEN` 対応の `isAllowedUser_`, `doGet(?probe)`, `token` 検証を追加して `clasp push`。Script Properties（`OPENCODE_API_KEY` 等）を既存からコピーしUIでデプロイ
2. `apps/app2/index.html` を `site/app2/index.html` に複製し `IS_GAS=false` でビルド。新 `site-app2-backend` の `/exec` を参照し、`probe` で生成UIの条件表示を確認（教室タブレット/先生で表示、教室外/未ログインで非表示）。削除ボタンは `isOwner_` のため先生のみ表示のまま
3. 生成→Drive保存→一覧表示→削除の一連を先生PCと教室タブレットで検証。保存先は既存と同一 `FOLDER_ID` のため新旧生成分は同一フォルダに混在

### Phase 4 — 旧ポータルGASの移行バナー追加（全Phase検証完了後）

1. `apps/portal/index.html` に `4.8` の移行バナー（`学習ツールポータルは新しいURLに移行しました...` + 新URLボタン）を追加。カードリンクは旧GASのまま残す
2. `clasp push` → UIでデプロイ更新（新バージョン）。旧ポータルは機能を維持したまま新URLへ誘導。旧URLは永久に有効

### 並行運用方針

- 全Phaseで新 `site/` + 新GAS2本と、旧 `apps/` + 旧GAS11本は**永久に並行稼働**。旧GASは一切停止・削除しない。新旧は同一 `DROP_TOKEN` / `DROP_FOLDER_ID` / `FOLDER_ID` を共有し保存先も共通。
- 新 `site/` の検証が完了するまでは旧ポータルのバナーは追加しない。

---

## 7. 検証方法

| 対象 | 方法 | 期待 |
|---|---|---|
| 匿名閲覧（Pages） | シークレットウィンドウで `https://.../app3/` 等を開く | ログイン要求なし、バーなし、200 |
| portal 未ログイン | 未ログインで `site/index.html` を開く | 「教室タブレットとしてログインする」ボタン表示（新 `site-pdfdrop` へリンク） |
| portal 教室タブレット | 教室Gmailで `site/index.html` を開く | 「教室タブレットとしてログイン中」表示（`!email_visible && allowed`） |
| portal 先生 | 先生PCで `site/index.html` を開く | 「先生アカウントでログイン中」表示（`email_visible && allowed`） |
| portal 教室外 | 教室外アカウントで `site/index.html` を開く | 「教室外アカウント」表示（`!allowed`） |
| app9 ボタン非表示 | 未ログイン/教室外で `site/app9/` を開く | 「教室タブレットで印刷」非表示 |
| app9 ボタン表示 | 教室Gmail / 先生PCで `site/app9/` を開く | probe成功で表示。押下→ `?probe` ok → PDF生成→新 `site-pdfdrop` に保存→ `last_ok` 更新 |
| app2 生成非表示 | 未ログイン/教室外で `site/app2/` を開く | 「生成設定」非表示、履歴と辞書のみ |
| app2 生成表示 | 教室Gmail / 先生PCで `site/app2/` を開く | 生成UI表示。生成→新 `site-app2-backend` 経由で `tadoku-*.json` が `FOLDER_ID` に作成（旧GAS生成分と同一フォルダに混在） |
| app2 削除ボタン | 先生PCで `site/app2/` を開く | 削除ボタン表示。教室タブレットでは非表示（`isOwner_` 維持。`!email → false`） |
| pdfdrop 診断 | 新 `site-pdfdrop /exec` を開く | `identity/masked/gate/limit/last_ok` が表示。`COUNT` が保存成功時のみ増加。旧 `apps/pdfdrop` も同様に有効 |
| 旧ポータルバナー | 旧 `apps/portal /exec` を開く（Phase4後） | ヒーロー下に「新しいURLに移行しました」バナー + 新URLボタン。旧カードは旧GASリンクのまま機能 |

---

## 8. 運用メモ（移行後）

- **トークン再生成時**: 新 `site-pdfdrop` / `site-app2-backend` と旧 `apps/pdfdrop` / `apps/app2` の全4GAS の Script Property `DROP_TOKEN` と、Pages側 `site/app9` / `site/app2` の定数を同時更新し `git push`。流用中の `e6135...` を変更する際は新旧全てを揃える。
- **ALLOWED_EMAILS 更新時**: 新旧4GASの両方を更新。可視の先生PCのみ列挙。教室Gmailは不可視のため不要。
- **辞典更新時**: `site-src/app1/content/*.md` をコミット → Actionsが `site/app1/bundle.json` を再生成。Drive編集フローは廃止（旧 `apps/app1` のDriveフォルダは残置するが参照されない）。
- **clasp禁止** (`AGENTS.md` 維持): `clasp deploy/redeploy` は404化するため禁止。`push` までとしデプロイ更新はUIで。新GAS2本も同様。
- **旧GASの扱い**: `apps/` 配下の11本のGAS・デプロイURLは永久に維持。`site/` 側が正本になっても旧ポータルのカード（旧GASリンク）は機能し続ける。新旧の保存先 `DROP_FOLDER_ID` / `FOLDER_ID` は同一のため、どちらから生成・投函しても同一フォルダに集約。
- **バックアップ**: `scripts/hooks/post-commit` の bundle 生成は継続。Pagesはgitが正本（`site/`）。

---

## 9. 付録: 変更ファイル一覧（予定）— `apps/` は永久残置・GASは新規作成

| ファイル | 操作 | 備考 |
|---|---|---|
| `仕様書.md` | 移行完了後に本書の内容で更新（旧版は `仕様書_GAS版.md` として保管） |  |
| `AGENTS.md` | 「静的Pages + 新規不可視GAS2本」構成、「起動時probeで条件表示」フローを追記。`site/` を正、`apps/` は旧GAS版として永久残置を明記 |  |
| `site/index.html`（新規） | `apps/portal/index.html` を複製。`qrUrl:544`, カード `href:337` をPages URLに、`notice:516` 上にログイン状態4パターン表示を追加 | 現行 `apps/portal/` は永久残置 |
| `site/app9/index.html`（新規） | `apps/app9/index.html:492,444` を複製し起動時probeに変更。新 `site-pdfdrop` の `/exec` を参照 | 現行 `apps/app9/` は永久残置（旧 `pdfdrop` 参照のまま） |
| `site-gas/site-pdfdrop/`（新規GAS） | `apps/pdfdrop/Code.js` を複製して新規scriptIdで作成。`DROP_TOKEN` は `e6135...` 流用 | 既存 `apps/pdfdrop` は永久残置 |
| `site-gas/site-app2-backend/`（新規GAS） | `apps/app2/コード.js:50,65,84` を複製し `isAllowedUser_` の `!email→true` 化、`DROP_TOKEN` 追加、`doGet(?probe)` 追加。新scriptIdで作成 | 既存 `apps/app2` は永久残置（旧URL用）。`site/app2/` は閲覧部のみ |
| `site/app2/index.html`（新規） | `apps/app2/index.html:595` を複製し `IS_GAS=false` 版をPages用にビルド。生成UIの条件表示を追加 | 削除ボタンは `isOwner_` のため先生のみのまま |
| `site/app1/index.html` + `bundle.json`（新規） | `apps/app1/Index.html` を複製し `fetch` 先を `bundle.json` に | `site-src/app1/content/` を正本に |
| `site/app3`〜`site/app8`（新規） | 各 `apps/appN/index.html` を複製（`Code.js` は不要） | 現行 `apps/` は永久残置 |
| `apps/portal/index.html`（既存改修・Phase4） | ヒーロー下に移行バナー（`学習ツールポータルは新しいURLに移行しました...` + 新URLボタン）を追加。カードリンクは旧GASのまま | 検証完了後に実施。旧GASは永久に有効 |

