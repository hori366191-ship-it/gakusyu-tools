# git bundle のバックアップファイル(Drive 同期フォルダへ移動後の残骸対策)
backup-*.bundle

# clasp の認証トークン(ホームディレクトリにあるが誤混入防止のため)
.clasprc.json

# Wrangler のローカルキャッシュ
site-worker/.wrangler/
site-worker/.dev.vars

# Windows のデスクトップ設定
desktop.ini

# 機密情報ロードマップ（ローカル専用・コミットしない）
ロードマップ_機密情報.md

# clasp scriptId（機密列挙防止・個別管理）
**/.clasp.json
