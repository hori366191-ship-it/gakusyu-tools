# 学習ツール システム仕様書

最終更新: 2026年8月25日

## 1. システム概要

Google Apps Script(GAS)で構成されたWebアプリ群。Google Workspace(hori-shota.com)上で運用され、塾の学習用ツールとして生徒(中学・高校生)に提供される。

| 構成要素 | 役割 |
|---|---|
| 学習ツール ポータル | 各アプリへの入口(リンク集)。単一ページのWebアプリ |
| 公式ビューア | Google Drive上のMarkdown教材をブラウザで閲覧するビューア |
| 英語長文 多読リーダー | AIで英検レベル別の英語長文を生成・閲覧するリーダー(教室内タブレット限定) |
| 正規分布シミュレーター | 正規分布の性質を視覚的に体験する物理シミュレーション |
| 立体図形ビューア | 立体の3D表示と展開図のアニメーション(2026-08-16 追加) |
| 原子・周期表ツール | 周期表・電子配置・結晶の可視化(2026-08-16 追加) |
| 微分積分ビジュアライザー | 微分・積分の可視化(2026-08-19 追加) |
| 月と星座シミュレーター | 月の満ち欠け・出南中入りと主要な星座(日周/年周・黄道)の可視化(2026-08-20 追加、2モード統合) |
| 英文法ビジュアライザー | 肯定文→否定文・疑問文・Wh疑問文への変形をアニメーションで観察する英語教材(2026-08-25 追加) |
| 原稿用紙メーカー(app9) | 行数×マス数を指定した原稿用紙をA4 1枚に生成し、印刷・PDF保存・教室タブレットへのプリント送信ができる(2026-08-25 追加) |
| PDF受信(pdfdrop) | 各アプリからPOSTされたPDFを印刷監視フォルダへ保存する汎用受信アプリ(2026-08-25 追加、ポータル非掲載) |

- 全アプリ共通: シンプルな2ファイル構成(`Code.js` + `index.html`)
- アクセス制御: アプリごとのデプロイ設定 + アプリ内コードのチェック

## 2. アプリ一覧

| アプリ | script ID | デプロイID | アクセス設定 | 用途 |
|---|---|---|---|---|
| 学習ツール ポータル | `YOUR_SCRIPT_ID_portal` | `AKfycbwJjosVgFh4c9d6bPjmLFnGjxDELkBSjs1WmZrvjC3ZdioT_GrZNyuqm7bDwunlPYxX` | 全員(誰でもアクセス可) | 各アプリへの入口 |
| 公式ビューア | `YOUR_SCRIPT_ID_app1` | `AKfycbzMNC1_ffakw1_1-zuYFNIhHj__NogqtjF--Yil-J4fRIW1i5BLYTF29dCNgkSCC2M2bg` | 全員(誰でもアクセス可) | Markdown教材の閲覧 |
| 英語長文 多読リーダー | `YOUR_SCRIPT_ID_app2` | `AKfycbxvoXt6H0_I1K5HOHCSYIPKR9PyoM-k8iXDNRPsxs3DsF0mtwE7AXgGNlxRE5d7eXZ4DA` | Googleアカウントでログイン | AI英語長文の生成(許可ユーザー)・閲覧(全員) |
| 正規分布シミュレーター | `YOUR_SCRIPT_ID_app3` | `AKfycbw8YE3GKPq4kYm9eC5Hs9L0F6iDwP9qa3iOJZcYMboE0PQkHvXYbI74nTAjuuPUDyNahg` | 全員(誰でもアクセス可) | 正規分布のシミュレーション |
| 立体図形ビューア | `YOUR_SCRIPT_ID_app4` | `AKfycbwoxjIDz7eumas4ucv_NI1iq3L1XY35CEh8qK2eE84k2wDdpwKyRPg75rto2OqrJLFuDA` | 全員(誰でもアクセス可) | 立体の3D表示と展開図 |
| 原子・周期表ツール | `YOUR_SCRIPT_ID_app5` | `AKfycbxm4owoBi8v9w_XDfcM5smWuWUNpfu1VgX0s4OupEW_6uxxwfGvT168cdvkCI8GFs3Y1w` | 全員(誰でもアクセス可) | 周期表・電子配置・結晶 |
| 微分積分ビジュアライザー | `YOUR_SCRIPT_ID_app6` | `AKfycbzkQRlKcO56aKij3mWDa6cwzdU4h5v3LpmfH8V5G3hQT6b1ALtVCnJ9Ebur5TiZ_gc8Nw` | 全員(誰でもアクセス可) | 微分・積分の可視化 |
| 月と星座シミュレーター | `YOUR_SCRIPT_ID_app7` | `AKfycbzK8bwWnRjeQfVUrKkuQrsLy1vjVk0Ico1-odRUhVPnoZ2v94rYLncOLeRjmMptfkJH` | 全員(誰でもアクセス可) | 月・星座の可視化(2モード) |
| 英文法ビジュアライザー | `YOUR_SCRIPT_ID_app8` | `AKfycbyuW_7Y4-VTF3zEudXis_ptWsa_YoQzl1Csu40pfWdl_0s5-w42pcM4LIMj6pz7-2ZcAQ` | 全員(誰でもアクセス可) | 英文法の変形アニメ観察 |
| 原稿用紙メーカー(app9) | `YOUR_SCRIPT_ID_app9` | `AKfycbxj4WNLi7qiQ9q1J1ynTDN1MiJ6IgVndQ0LHpOPFZObKSi88hIU6uOKMmCUUADQr2vs` | 全員(誰でもアクセス可) | 原稿用紙の生成・印刷・プリント送信 |
| PDF受信 pdfdrop | `YOUR_SCRIPT_ID_pdfdrop` | `AKfycbxP6klWI4L5wnTl2wTO5PmrDMf6z4Fmu3Qh3zFwfCf0KUa91idOINv6w8PxoY3Ke-oN` | Googleアカウントでログイン | 汎用PDF受信(ポータル非掲載) |

※デプロイバージョンは更新のたびに増加する(2026年8月20日時点: ビューア@11、多読リーダー@14、シミュレーター@4、立体ビューア@1、周期表@3、微分積分@1、月@1)。

## 3. 各アプリ仕様

### 3.1 学習ツール ポータル

- 配信: `doGet()` が `index.html` を返す(タイトル「学習ツール」、`XFrameOptionsMode.ALLOWALL`設定済み)
- デザイン(モダン・ミニマル、2026-08-25 リニューアル):
  - フォント: **Zen Kaku Gothic New**(Google Fonts CDN)
  - ヒーロー: アイボリー系パステルの縦グラデ(`#fefdf9→#f8f1de`)+ 幾何学グリッドパターン(28px間隔の濃紺細線)+ **ワイヤーフレーム多面体アニメーション**(canvas実装、二十面体/立方体/正四面体/八面体の4図形を非対称配置、濃紺半透明`rgba(30,42,74,.55)`、回転+浮遊、`prefers-reduced-motion`対応)。ヒーロー内の文字はすべて濃色(タイトル濃紺`#1e2a4a`、kicker濃インディゴ`#4338ca`、サブ・QRキャプションスレート`#52607a`)
  - ヒーロー文言: `LEARNING TOOLS` / `学習ツール` / `教室長自作の学習ツール`
  - **QRコード**(2026-08-16 追加、2026-08-25 濃紺化): ヒーロー右下に濃紺QR(`#1e293b`、背景透過、モバイルは縮小表示。アイボリー背景では白QRが読めないため)。ポータル自身のURL(`/a/`形式)にリンクし、教室のスマホから開く用。`qrcode-generator`ライブラリ(MIT)をindex.htmlにインライン埋め込み、実行時にSVG描画。URLは描画スクリプト冒頭の`qrUrl`定数1箇所で管理。ヒーローはflex縦並びで、QRは必ず最終文言行の下に追従(絶対配置なし)。サイズ: デスクトップ98px/モバイル49px(エラー訂正L・セル2px、1x画面でもスキャン可能な下限)
  - **セクション構成**(2026-08-25 追加): カードを4セクションにグループ化。順序は使用頻度順で **①ツール(公式ビューア・原稿用紙メーカー)→②英語(多読リーダー・英文法)→③数学(正規分布・立体図形・微分積分)→④理科(原子周期表・月と星座)**。見出しは教科色の縦バー+日本語タイトル+英字サブタイトル(Tools/English/Mathematics/Science)
  - カード: 各セクション内にグリッド配置。**アイコンと「開く」ボタンは教科色で統一**(茶`#d3b491→#a67c52`/緑`#93d3a5→#4d9e68`/青`#85b8ec→#4a7fc1`/紫`#b7a6ee→#8464c8`のパステルグラデ、クラスは`subj--tool/eng/math/sci`)。角丸14px、ホバーで浮上
  - **学年タグ**(2026-08-25 追加): 各カード右上に無彩色ピル型バッジ(淡グレー地+スレート文字、`card__grade`)。「小学生以上」「中学生以上」「高校生以上」の3種。教科色と混同しないよう無彩色で統一
  - レスポンシブ: 幅900px以下で1列に切り替え
- リンク: 全て `/a/`形式URL + `target="_blank"`(多ログイン環境でも動作するため)

### 3.2 公式ビューア

- 機能: Driveフォルダ内の`.md`ファイル一覧・内容をブラウザで閲覧
- 動作方式: **バンドル方式**(ページロード時に一覧+全コンテンツをHTMLに埋め込み、以降の切り替えはブラウザ内で完結。閲覧ごとのGAS実行なし)
- 設定定数(`Code.js` 先頭):

| 定数 | 値 | 説明 |
|---|---|---|
| `DRIVE_FOLDER_ID` | `1wrajAftZ5r5i0Yn5SNlhMq1fG-rDIVS3` | MDファイル置き場のDriveフォルダ |
| `FILE_LIST_CACHE_TTL_SECONDS` | 1800 | ファイル一覧キャッシュ(30分) |
| `CONTENT_CACHE_TTL_SECONDS` | 600 | コンテンツキャッシュ(10分) |
| `VALIDATION_CACHE_TTL_SECONDS` | 3600 | アクセス検証キャッシュ(1時間) |

- クエリAPI(GET):

| パラメータ | 戻り値 |
|---|---|
| `?action=getMdFilesList` | フォルダ内の.md一覧(JSON) |
| `?action=getMdContent&id=<fileId>` | 指定ファイルの内容(JSON) |
| `?action=getBundle[&refresh=1]` | 一覧+全コンテンツのバンドル(JSON) |
| 上記以外 | フロントエンド(`Index.html`) |

- セキュリティ: `isAllowedFile()` で「設定フォルダ内の`.md`ファイルのみ」を検証(結果はキャッシュ)
- キャッシュ: `CacheService.getScriptCache` 使用

### 3.3 英語長文 多読リーダー

- 機能: 英検レベル(7級〜1級)別の英語長文をAI生成し、Driveに保存・一覧閲覧・削除
- アクセス権限(3段階、2026-08-16 変更):
  1. サイト閲覧・生成済み長文の閲覧・辞書引き: **Googleアカウントがあれば全員可**(デプロイ設定: 実行ユーザー=アクセスしているユーザー / アクセス=Googleアカウントでログイン)
  2. AI生成: **許可ユーザーのみ**。`Session.getActiveUser().getEmail()` を Script Properties `ALLOWED_EMAILS` と照合(サーバー側 `generateText()` でも検証)。許可外は生成UI自体が表示されない
  3. 生成物の削除: **オーナーのみ**。Script Properties `OWNER_EMAIL` と照合(サーバー側 `deleteText()` でも検証)。許可外は削除ボタン非表示
- Script Properties:

| キー | 説明 |
|---|---|
| `OPENCODE_API_KEY` | AI生成APIのキー |
| `ALLOWED_EMAILS` | AI生成が可能なGoogleアカウント(カンマ区切り) |
| `OWNER_EMAIL` | オーナーアカウント(生成物の削除権限。2026-08-16 追加) |
| `FOLDER_ID` | 生成物の保存先フォルダ(未設定時は「生成済み長文」を自動作成) |

- Drive共有(必須):
  - 生成物フォルダ(`FOLDER_ID`): リンクを知っている全員=閲覧可 / 許可ユーザー=編集者(個別追加)
  - 辞書 `DICTIONARY_ID` / `MAP_ID`: リンクを知っている全員=閲覧可
- Drive連携(コード内定数):

| 定数 | ID |
|---|---|
| `DICTIONARY_ID` | `13G_bvPbBaoFx-XC9qJAaJTBP66G_-EdK` |
| `MAP_ID` | `1J4_grvw-tZKV1BLqgVlCGb8A5Oa2hEdx` |

- AI生成: `https://opencode.ai/zen/go/v1` / モデル `deepseek-v4-flash` / 最大3回リトライ(形式違反時は再指示付き)
- 辞書読み込み: ページロード時に `getDICTIONARY`(辞書本体)と `getFormMap`(語形正規化表)を並列で先読み(単語タップ時の待ち時間をなくす。先読み失敗時はタップ時に再取得)
- 保存形式: `tadoku-<timestamp>.json`(タイトル+本文+設定)、削除はゴミ箱送り

### 3.4 正規分布シミュレーター

- 機能: 正規分布の物理シミュレーション(canvas描画、アスペクト比880/660)
- `doGet()`: `index.html` を返す(タイトル「正規分布 物理シミュレーター」、`ALLOWALL`設定済み)
- 実行制御(2026-08-16 追加):
  - ページを開いても自動では動かず、キャンバス上のオーバーレイ「▶ スタート」(または実行制御カードのボタン)を押してから開始する
  - キャンバス全体がトグルボタン: 実行中にクリックで停止(オーバーレイが「▶ 再開」で再表示)、再クリックで再開
  - 初回スタートはリセットと同等(`G.build(false)` + `R.build(false)`)で、開始前は弾(玉・粒子)を配置しない(RW は `seeded` フラグで粒子生成を開始まで保留)

### 3.5 立体図形ビューア

- 機能: 立体の3D表示(canvas手書き、外部ライブラリなし)と展開図アニメーション
- `doGet()`: `index.html` を返す(タイトル「立体図形ビューア」、`ALLOWALL`設定済み)
- 図形12種: 正四面体・立方体・正八面体・正十二面体・正二十面体・三角柱・四角柱・六角柱・四角錐・円柱(24角形近似)・円錐(24角形近似)・球(経緯線近似)
- 表示モード: ワイヤーフレーム(**デフォルト**)/ 半透明ソリッド(面の明暗+色分け)/ エッジ+面。操作: ドラッグ=回転、ピンチ/ホイール=ズーム、自動回転トグル(**デフォルトOFF**)
- 回転操作(2026-08-16 変更): **トラックボール方式**。回転を永続3×3行列で保持し、ドラッグ増分を画面軸(ビュー空間)で左掛けするため、図形の向きによらず左右・上下とも指の動きに追従する(従来のyaw/pitch方式では裏返しの向きで上下が反転して見えた)。Gram-Schmidt直交化で数値ドリフト防止。展開開始時、展開図の平面が視点とほぼ平行(エッジオン)な場合は自動で正面に向く(0.8秒のアニメーション)
- 投影(2026-08-16 変更): **平行投影(遠近法なし)がデフォルト**(数学の問題図の表記に合わせる)。「遠近法」スイッチで透視投影に切替
- 曲面の扱い(円柱・円錐・球): 近似面の分割線(隣接面の法線角<25°の辺)は立体表示では描画しない。**曲面と背景の境界線(シルエット)は「前面/背面の境目」ルールで常時描画**: 隠し辺を挟む2面の現在の法線(展開途中も含む)が視線に対して表裏に分かれる辺を輪郭線として描く。これにより円柱=輪郭母線2本・円錐=頂点から底面円への接線2本が**多角形近似面の厳密な境界位置**に自動的に現れ、展開アニメ中も巻き残った曲面の境界線が描かれる(2026-08-16 修正: 従来の解析式は円錐で頂点高さのオフセット項を落としており、接線が底面楕円の最左端・最右端にずれていた)。球は内部の経緯線を出さず**輪郭円のみ**をビュー空間で厳密に描画(平行投影=中心深度の円、遠近法=接円z₀=R²/D・半径R√(1−(R/D)²))。**展開時は切り離された分割線(切り口)を表示**(2面の共有頂点位置が離れているかを判定)。ワイヤー表示は背面の線(例: 円柱底面の裏側の弧)も含め全可視辺を実線で描画(塗りは前面のみ)。球・円柱・円錐は面をグループ色で着色(球=単色、円柱・円錐=側面1色+底面別色)。閉じた状態では背面カリングで塗りを前面のみに
- 正二十面体・正十二面体: 正二十面体は緯度構築(帯の三角形接続修正済み)、正十二面体はその双対として生成。全辺長が等しいことを検証済み。球は極を最北・最南リングに正しく接続(2026-08-16 修正)
- 展開図モード:
  - 汎用ヒンジ展開アルゴリズム(`unfoldGeo`): 面の隣接グラフの全域木ごとに、共有辺を軸に面を倒して平面に展開。開閉スライダー+展開/組み立てアニメーション
  - **キャンバス右下にフローティングボタン**(▶展開/◀組み立て、48px円形)を常時表示(モバイルでスクロールせず操作可能。展開図がない図形では無効化)(2026-08-16 追加)
  - **自動フィットのズームアウトを展開中は半分に減衰**(立体サイズと展開図サイズを1:1で補間)。展開図は従来の約2倍の大きさで表示(2026-08-16 変更)
  - 折り角の符号は「±β回転の2候補のうち親法線との内積が大きい方」を採用(小さい折り角でも正しく平らになる)
  - 展開パターンは**実行時に自動列挙+重なり検査+重複除去**で生成(`allTrees`+`polyOverlap`+`canonicalSig`)。立方体=11通り、正四面体=2通り、正八面体=11通り(正規の全パターンと一致)。`allTrees`は**バックトラッキングで全域木のみ列挙**(全ビットマスク走査の高速化)
  - 角柱は**「側面の帯+底面2箇所」の標準パターンを全列挙**(三角柱=4通り・四角柱=6通り・六角柱=12通り)。四角錐は**全全域木の列挙**(8通り)。円柱・円錐は標準的な展開図を1つ用意(円柱=長方形+円2つ、円錐=扇形+円)。球・正十二面体・正二十面体は展開図なし(メッセージ表示)(2026-08-18 変更)
  - 重複除去(`canonicalSig`)は**辺方向の全ペア差を回転候補**にして合同な展開図を正確に統合(2026-08-18 修正)。従来の`canonStep`(辺方向最小間隔の倍数のみ試行)では四角錐で合同な図形が統合されず27通りに誤計上された。修正後は四角錐=8通り、立方体=11・正八面体=11・正四面体=2・角柱4/6/12は不変
  - **全展開図一覧モード(2026-08-18 追加)**: 「展開図」カードの「全展開図一覧」ボタンで、全パターンをcanvasグリッドに縮小して一覧表示(2通り以上ある図形のみ有効。セル下部に通し番号)。一覧中は**ドラッグ回転・自動回転・ホイールズームを無効化**。視点は**アニメ進行に応じて自動調整**(2026-08-18 変更): 組み立て時(閉)は正面+上面+側面の見える**3/4視点**(面0の法線を整列後に pitch0.55×yaw0.55)、展開時(開)はネットが読みやすい**ほぼ正面視点**(pitch0.3×yaw0)、開閉アニメ中は角度を滑らかに補間。フィット倍率は視点と展開の両方をグリッドサンプリングして算出(はみ出しなし)。開閉スライダー/▶◀ボタンは**全パターン共通の進行率で一斉アニメーション**。セルをクリックすると単体表示に切替えそのパターンを選択。一覧開始時は全展開済み
- 学習メモ: 面F・頂点V・辺Eの数、オイラーの定理(V−E+F=2)の成立判定、体積・表面積の公式(KaTeX)

### 3.6 原子・周期表ツール

- 機能: 周期表・電子配置・結晶の可視化(canvas描画、外部ライブラリなし)
- `doGet()`: `index.html` を返す(タイトル「原子・周期表ツール」、`ALLOWALL`設定済み)
- 3タブ構成:
  - **周期表**: 元素をタップすると右側に詳細表示。分類(アルカリ金属・ハロゲン等)と s/p/d/f ブロックの色分けを切替可能
  - **原子モデル**: 電子配置をボーア模型風に描画(実際は原子核の周囲に確率的に広がる「電子雲」である旨の注記付き)。最外殻の電子(価電子)を強調表示
  - **結晶**: 結晶構造の3D描画

### 3.7 微分積分ビジュアライザー

- 機能: 微分・積分の可視化(canvas描画、KaTeX数式表示、外部ライブラリ不要の手書き描画)
- `doGet()`: `index.html` を返す(タイトル「微分積分ビジュアライザー」、`ALLOWALL`設定済み)
- 2タブ構成: **微分** / **積分** (右パネルのセグメントで切替)
- 対応関数5種:
  | 関数 | 表示名 | パラメータ | x範囲 |
  |---|---|---|---|
  | `quad` | 2次関数 `f(x)=a x²+b x` | a(-2〜2), b(-4〜4) | -6〜6 |
  | `cubic` | 3次関数 `f(x)=a x³+b x` | a(-0.3〜0.3), b(-3〜3) | -5〜5 |
  | `sin` | 三角関数 `f(x)=A sin(Bx)` | A(-3〜3), B(0.5〜3) | -6〜6 |
  | `exp` | 指数関数 `f(x)=e^(kx)` | k(-1.2〜1.2) | -3.5〜3.5 |
  | `log` | 対数関数 `f(x)=a ln(x)` | a(-3〜3) | 0.05〜6 (定義域 x>0) |
- 微分タブ:
  - メインcanvas: 関数 `f(x)` + 接線(金) + 割線(赤、x₀→x₀+h) + 接点マーカー
  - サブcanvas: 導関数 `f'(x)` (緑) + 現在の微分係数の点
  - 操作: 接点 `x₀` スライダー、割線幅 `h` スライダー(0.02〜3)、「▶ 極限を見る(h→0)」ボタンで `h` を指数減衰(`h -= dt*h*2.2`)でアニメーション
  - 表示: 微分係数 `f'(x₀)`、増減判定(増加/減少/極値候補)、KaTeXで `f(x)`/`f'(x)`/`F(x)` を表示
- 積分タブ:
  - メインcanvas: 関数 `f(x)` + 符号付き面積(正=紺透過、負=赤透過) + リーマン和の長方形(左端/中点/右端) + 区間端 `a`/`b` の破線
  - サブcanvas: 面積関数 `S(x)=∫ₐˣ f(t)dt` (紫) + 現在の `S(b)` の点。`a` は固定下端、横軸は `a`〜`xmax`
  - 操作: 下端 `a` / 上端 `b` スライダー(相互制約 `b-a>=0.2`)、分割数 `n` スライダー(2〜120)、高さモード(左端/中点/右端)、「▶ 収束を見る(n→∞)」ボタンで `n += dt*30` でアニメーション(上限120)
  - 表示: 定積分 `∫ₐᵇ f(x)dx`、リーマン和、誤差、面積関数値 `S(b)`。基本定理 `S'(x)=f(x)` の注記
- y軸制御(2026-08-20 変更): 初回表示時のyレンジを保存(`getInitMain`/`getInitSubDiff`/`getInitSubInteg`)し、パラメータ変更時の目標レンジへは減衰 `DAMP=0.15` で追従(`dampedRange`)。即時追従ではなく形状変化を持続させつつ発散を防止。サンプルは320点でレンジ算出、グリッドは `niceStep` で自動目盛り
- レイアウト: 左がメインcanvas(880/470) + サブcanvas(880/230)、右が操作パネル(336px)。画面幅820px以下で1列に切替。ヘッダー/フッターは `Noto Serif JP`、KaTeX 0.18.4 CDN

### 3.8 月と星座シミュレーター（2モード統合）

- 機能: 月の満ち欠け・出/南中/入りと主要な星座(日周/年周・黄道)の可視化(canvas手書き、外部ライブラリなし)
- `doGet()`: `index.html` を返す(タイトル「月と星座シミュレーター」、`ALLOWALL`設定済み)
- 共通2画面構成: 上=宇宙視点、下=地球視点（地平線・東西南北）。上段と下段は同期し、右パネル上部の「月/星座」セグメントでモード切替
- **月モード:**
  - 上=北極側俯瞰（太陽=左側の平行光線、地球中心、月軌道、公転角の弧）、下=南の空（夜空の月）
  - 操作: 月齢スライダー(0〜29.5日, 0.1刻み)、8相ボタン(新月/三日月/上弦/十三夜/満月/十八夜/下弦/有明月)、▶アニメ(29.5日を18秒で一周、×1/×3速、逆再生)
  - 表示: 月相名(朔/望つき)・輝面率`(1-cosθ)/2`・位相角、輝面は右側から太り右側から痩せる(北半球)。満月/新月は全面、上弦/下弦は半分、他はterminatorが曲線
  - 時刻: 位相角から `南中=12+θ/15`、 `出=南中-6`、 `入り=南中+6` を24hで算出、毎日約50分遅れを可視化。下側のタイムラインに24h目盛りと出/南中/入り＋現在時刻マーカー
  - 観測者位置: 時刻スライダー(0〜24h, 0.25刻み)で地球上の観測者を回転(12h=西/昼, 18h=南, 0h=東/夜, 6h=北)。宇宙図の橙点が地球円周上を移動。空の月は時角 `ha=time-南中`（-12〜12h正規化）で東→南（南中最高）→西へ移動、|ha|>6hで地平線の下に沈む。南中/19時のワンクリック移動ボタン付き
  - オプション: 同期自転(同じ面を向けるクレーター表示)、8相ゴースト、食の説明(一直線で日食/月食、傾き5°で普段は外れる旨)
- **星座モード:**
  - 上=黄道・公転俯瞰（太陽中心、地球が黄道上を公転、夜側扇、外周に十二支帯と四季ラベル）、観測者から東西南北の矢印（北=赤・東=緑・南=黄・西=青）
  - 下=観測者の空（方位をドラッグで全周360°パン、ホイールでも微回転、方位グリッド表示可）。地平線・山シルエット、天頂、子午線の概念を可視化。北極星は高度=緯度で固定
  - 星表: 主要12星座（北斗七星・カシオペヤ座・しし座・はくちょう座・こと座・わし座・ペガスス座・オリオン座・おおいぬ座・ふたご座・さそり座・夏の大三角/冬の大三角）の約36星（等級と色で大きさ・色分け、1等星は名前表示）。黄道(黄破線)と天の赤道(灰破線)を重ねて表示可
  - 操作: 日付スライダー(1〜365日, 春分80/夏至172/秋分266/冬至356の4ボタン、▶年周アニメ 365日/18秒)、時刻スライダー(0〜24h, ▶日周アニメ 24h/10秒、×1/×3速、逆再生)、ドラッグで方位 `viewAz` を変更（南中心から東西南北すべて、±180°）
  - 計算: 太陽黄経 `sunRA=(date-80)*360/365`、恒星時 `LST=sunRA+(time-12)*15`、時角 `H=LST-RA`、高度 `sinAlt=sinDec sinLat+cosDec cosLat cosH`、方位 `az=atan2(-cosDec sinH, sinDec - sinAlt sinLat)`（北0°基準）。緯度は東京35°固定（スライダ不要）
  - 表示: 南に見える季節と代表星座、方位、恒星時、視界内の星数、北極星高度。日付で1ヶ月30°=2hの年周ズレを数値で実感
- 注記: 距離・大きさは誇張、角度のみ正しい。月も星座も「昼間も存在し太陽光で見えないだけ」・「地球の影が原因ではない」ことを明記

### 3.9 英文法ビジュアライザー

- 機能: 肯定文をベースに、ボタン操作で否定文・疑問文・Wh疑問文への変形をアニメーションで観察する英語教材(入力操作なしの観察特化型)
- `doGet()`: `index.html` を返す(タイトル「英文法ビジュアライザー」、`ALLOWALL`設定済み)
- 構成: 文法マップ(根=be動詞/一般動詞、幹=三単現/be過去/一般過去、枝=Wh疑問詞。助動詞・進行形・現在完了は準備中グレーアウト) + メインステージ
- メインステージ: 上段=肯定文カード列(品詞色分け: 主語=青/動詞=赤/助け舟=黄/その他=灰、日本語訳・凡例つき) → 中段=変形ボタン → 下段=固定スロット(否定文/疑問文。Wh選択時はYes/No+Where/What/Who/Whenの5スロット)
- アニメ: 共通プリミティブ(カード挿入=FLIP+落下、縮約合体=not吸い込み、語順入替=弧を描くFLIP、句点→?ポップ、s/ed剥離=幅収縮で原形化)。全演出に速度係数 `SPEED=1.6`(CSSは `--spd`)を適用
- 変形パターン: be動詞+not→縮約(isn't/aren't/wasn't/weren't、amは非縮約)、do/does/did+not→don't/doesn't/didn't、be疑問=be動詞が文頭へ、一般疑問=Do/Does/Did着地+主語小文字化
- Wh疑問詞(2段階): 肯定文→Yes/Noスロット生成(保持)→Yes/No行をコピーしてWh挿入。Where/When/Whatは対象カード(place/time/obj)が存在する場合のみハイライト→消滅(存在しなければ消滅スキップ)。Whoは例外でDoes/Didを使わず主語をWhoに置換(動詞のs/edは残す)。Wh挿入後、文頭でなくなった助け舟(Does/Did/be動詞)は小文字化
- 例文: ノードごとに2〜6文をデータ駆動(`NODES`定数)で切替可。Whノードは現在2+過去2+be現在1+be過去1の計6文
- ノートタグ: 「✎ ノートに書こう!」を肯定文の直後(下端揃え)・各変形文の直後・POINT文末(インライン)に表示。淡オレンジ(#fff7ed)
- 学習補助: 変形完了時にPOINT(一言解説)+日本語訳をフェードイン。ボタン再押下でリプレイ
- 技術: DOMカード+Web Animations API(外部ライブラリなし)、Zen Kaku Gothic New(Google Fonts CDN)、レスポンシブ(820px切替)

### 3.10 原稿用紙メーカー(app9)

- 機能: 1行のマス数(1〜50)×行数(1〜50)を指定した原稿用紙を**A4 1枚ぴったり**に生成。印刷ボタンまたは教室タブレットへのプリント送信
- `doGet()`: `index.html` を返す(タイトル「原稿用紙作成」、`ALLOWALL`設定済み)。全処理はクライアント側でサーバー関数なし
- 書き方向: **横書きがデフォルト**(縦書きは右→左へ列配置)
- A4フィット計算: マス1辺=`min(190mm÷マス数, 本文高さ÷行数)`(小数第2位切捨て)。題名・氏名欄ON時は本文高さから24mmを先に確保。`@page { size:A4; margin:10mm }`、印刷時は設定パネル非表示・シートのpadding解除
- 罫線色: スウォッチ5色(薄緑`#bcd0c6`定番/グレー/薄青/薄ピンク/ベージュ)+カスタムピッカー。CSS変数`--line-color`で即時反映
- プリセット: 400字詰め(20×20)/200字詰め(20×10)/600字詰め(30×20)/100字詰め(10×10)
- 設定の永続化: localStorage(`genkoshi-settings-v2`)に方向/マス数/行数/氏名欄/罫線色を自動保存し次回復元(数値クランプ・形式チェック、storage無効環境では無害にスキップ)
- **教室タブレットで印刷ボタン**(GAS環境のみ表示、ローカルfile://では自動非表示):
  - **送信前認証プローブ**: pdfdropの `doGet ?probe=1&token=&callback=` をJSONPで叩く(4秒ウォッチドッグ)。無応答=未ログインとみなし「ログインページを開く」リンク付きエラーを約4秒で表示 / 許可外の可視アカウントはマスク済みアドレス付きで即拒否
  - html2canvas(scale2)+jsPDFでシート全体をPDF化。画像は**JPEG(q0.92)固定**(PNG埋め込みはjsPDFで異常膨張する実測のため)
  - 隠しiframeフォームPOST(`text/plain` JSON `{token,name,b64}`)でpdfdropへ送信
  - 着弾確認: pdfdropのJSONPポーリング(`doGet ?poll=<名>&token=&callback=`, 3秒×20回)が主経路 + postMessage(top+parent二重送信)が副経路。成功時「送信完了: <ファイル名>」。ポーリングが3回連続無応答でも認証エラー表示に切替(送信中のログアウト対策)
- クレジット: hint部に jsPDF・html2canvas(MIT License) の表記

### 3.11 PDF受信(pdfdrop)

- 機能: アプリからPOSTされたPDFをDriveフォルダへ保存するだけの汎用受信アプリ。ポータル非掲載。送信側はapp9(他アプリからの再利用も想定)
- `doPost()`: `e.parameter.data`(JSON `{token,name,b64}`)を処理 → トークン照合 → emailゲート → サイズ上限10MB → `%PDF`マジック検査 → `Folder.createFile()` で保存 → 応答HTML(postMessage用script埋め込み)
- アクセスゲート:
  - `DROP_TOKEN` 不一致は常に拒否
  - email可視アカウント(hori-shota.com等): `ALLOWED_EMAILS`照合(リスト空なら全員可)
  - **email不可視アカウント(Gmail系等)**: GAS仕様によりemail取得不可のため、トークンのみで許可(教室タブレット=Gmail運用のためこの設計)
- `MAX_PER_DAY`: 日次受信上限(既定200、Script Propertyで変更可)。**保存成功時のみ消費**(`COUNT_yyyy-MM-dd`キー管理、過去キー自動削除)。超過時は `daily limit reached (n/day)`
- 診断: 成功時`LAST_OK`・失敗時`LAST_ERROR`をScript Properties記録し、ステータスページ(doGet)に表示。実行ログにもconsole出力。障害時はまずステータスページの`last_error`を見る
- doGet:
  - 通常アクセス → ステータスページ(identity/gate/token/folder/limit/last_ok/last_error)
  - `?poll=<ファイル名>&token=&callback=<cb>` → JSONP応答 `cb({genkoPoll,found,name})`(フォルダ内同名ファイル存在確認。callback名は英数字制限)
  - `?probe=1&token=&callback=<cb>` → JSONP応答 `cb({genkoProbe,ok,email_visible,masked,allowed})`(送信前の認証・許可チェック用)
- Script Properties:

| キー | 説明 |
|---|---|
| `DROP_FOLDER_ID` | PDF保存先(印刷監視フォルダ。別アカウント所有分をオーナーに編集者共有して使用) |
| `DROP_TOKEN` | 送信側と共有するシークレットトークン |
| `ALLOWED_EMAILS` | 可視アカウントの許可リスト(カンマ区切り。空ならemailゲートOFF) |
| `MAX_PER_DAY` | 日次上限(省略時200) |

- デプロイ: アクセス「Googleアカウントでログイン」/ 実行「自分」。manifestは `oauthScopes: drive`

## 4. URL一覧

共有用URLは**`/a/`形式**(ドメイン指定)を使用する。複数Googleアカウントにログイン中の環境でも安定して動作する。

| アプリ | 共有URL(`/a/`形式) |
|---|---|
| 学習ツール ポータル | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbwJjosVgFh4c9d6bPjmLFnGjxDELkBSjs1WmZrvjC3ZdioT_GrZNyuqm7bDwunlPYxX/exec` |
| 公式ビューア | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbzMNC1_ffakw1_1-zuYFNIhHj__NogqtjF--Yil-J4fRIW1i5BLYTF29dCNgkSCC2M2bg/exec` |
| 英語長文 多読リーダー | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbxvoXt6H0_I1K5HOHCSYIPKR9PyoM-k8iXDNRPsxs3DsF0mtwE7AXgGNlxRE5d7eXZ4DA/exec` |
| 正規分布シミュレーター | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbw8YE3GKPq4kYm9eC5Hs9L0F6iDwP9qa3iOJZcYMboE0PQkHvXYbI74nTAjuuPUDyNahg/exec` |
| 立体図形ビューア | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbwoxjIDz7eumas4ucv_NI1iq3L1XY35CEh8qK2eE84k2wDdpwKyRPg75rto2OqrJLFuDA/exec` |
| 原子・周期表ツール | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbxm4owoBi8v9w_XDfcM5smWuWUNpfu1VgX0s4OupEW_6uxxwfGvT168cdvkCI8GFs3Y1w/exec` |
| 微分積分ビジュアライザー | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbzkQRlKcO56aKij3mWDa6cwzdU4h5v3LpmfH8V5G3hQT6b1ALtVCnJ9Ebur5TiZ_gc8Nw/exec` |
| 月と星座シミュレーター | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbzK8bwWnRjeQfVUrKkuQrsLy1vjVk0Ico1-odRUhVPnoZ2v94rYLncOLeRjmMptfkJH/exec` |
| 英文法ビジュアライザー | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbyuW_7Y4-VTF3zEudXis_ptWsa_YoQzl1Csu40pfWdl_0s5-w42pcM4LIMj6pz7-2ZcAQ/exec` |
| 原稿用紙メーカー(app9) | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbxj4WNLi7qiQ9q1J1ynTDN1MiJ6IgVndQ0LHpOPFZObKSi88hIU6uOKMmCUUADQr2vs/exec` |
| PDF受信 pdfdrop(非掲載) | `https://script.google.com/a/macros/hori-shota.com/s/AKfycbxP6klWI4L5wnTl2wTO5PmrDMf6z4Fmu3Qh3zFwfCf0KUa91idOINv6w8PxoY3Ke-oN/exec` |

平形式(`https://script.google.com/macros/s/<デプロイID>/exec`)も動作するが、多ログイン環境でエラーになることがあるため共有用途には`/a/`形式を使用する。

## 5. デプロイ・更新手順

### 更新フロー(コード変更時)

1. ローカルの対象フォルダ(`apps/<アプリ>`)のコードを編集
2. `clasp.cmd push -f` でアップロード(HEADが更新される)
3. **GASエディタUIでデプロイを更新**(下記) — この操作は人間がブラウザで行う

```
GASエディタ → デプロイ → デプロイを管理
→ 対象デプロイの編集(鉛筆アイコン)
→ バージョン: 「新しいバージョン」を選択 → デプロイ
```

- デプロイURLはバージョン更新後も**不変**
- 新規デプロイ作成時: デプロイ → 新しいデプロイ → 種類: ウェブアプリ → 実行ユーザー/アクセス設定を選択 → デプロイ

### ⚠️ 重要: claspでのデプロイ操作禁止

- `clasp deploy` / `clasp redeploy` は**使用しないこと**
- claspが作成・更新するデプロイは**webAppエントリポイント(アクセス設定)が失われ、実行URLが404**になる(実測済み)
- アクセス設定の変更はGASエディタUIからのみ可能(Apps Script APIでも設定不可)

## 6. ディレクトリ構成

```
E:\dev\学習ツール\
├── apps\
│   ├── app1\        … 公式ビューア (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── Index.html
│   ├── app2\        … 英語長文 多読リーダー (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── コード.js
│   │   └── index.html
│   ├── app3\        … 正規分布シミュレーター (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── index.html
│   ├── app4\        … 立体図形ビューア (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── index.html
│   ├── app5\        … 原子・周期表ツール (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── index.html
│   ├── app6\        … 微分積分ビジュアライザー (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── index.html
│   ├── app7\        … 月と星座シミュレーター (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── index.html
│   ├── app8\        … 英文法ビジュアライザー (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── index.html
│   ├── app9\        … 原稿用紙メーカー (script: YOUR_SCRIPT_ID...)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── index.html
│   ├── pdfdrop\     … PDF受信アプリ (script: 1DtFS1w-, ポータル非掲載)
│   │   ├── .clasp.json
│   │   ├── appsscript.json
│   │   ├── Code.js
│   │   └── (index.htmlなし。doGetは動的HTML生成)
│   └── portal\      … 学習ツール ポータル (script: YOUR_SCRIPT_ID...)
│       ├── .clasp.json
│       ├── appsscript.json
│       ├── Code.js
│       └── index.html
├── その他\          … 実験・非公開アプリの開発場所(ポータル未掲載・GAS非プッシュ)
├── scripts\         … gitフック(post-commitバックアップ)・backup.ps1
├── 仕様書.md        … 本ファイル
└── AGENTS.md        … AIエージェント向け作業ガイド
```
